const classService = {
  joinClass: async (code) => { 
    return {}; 
  },
  getMyClasses: async () => { 
    return []; 
  },
  getClassById: async (id) => {
    return {};
  }
};
export default classService;
